---
interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle } = Astro.props;
---

<div class="mb-12">
  <h1 class="text-3xl sm:text-4xl font-bold tracking-tight text-white">{title}</h1>
  {subtitle && <p class="mt-3 text-lg text-slate-400 max-w-2xl">{subtitle}</p>}
</div>
